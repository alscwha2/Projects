from typing import List
from sys import argv as argv

'''
	Any time you can do a replace, do it
	Find the letters in word 1 that occur in order
		ther may be multiple sub-sequences
	number of operations = 
		changes: number of wrong letters in the right positions in the subsequence
		insertions: number of holes
		deletions: number of extra letters where no letter should belong
'''

class Solution:
	def minDistance(self, word1: str, word2: str) -> int:
		subsequences = []
		# find all subsequencces List[list]

		# analyze each subsequence
		for subsequence in subsequencces:
			
		

# argv[1]
# print(Solution())
